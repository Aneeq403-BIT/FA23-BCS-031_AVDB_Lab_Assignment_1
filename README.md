# GoodBooks API (MongoDB + FastAPI)

A production-style REST API built for the GoodBooks-10k dataset using Python FastAPI and MongoDB.

## Prerequisites
- Docker & Docker Compose installed on your machine.

## Quick Start

1. **Start the services:**
   ```bash
   docker-compose up -d --build